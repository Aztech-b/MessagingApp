import { createTheme } from "@mantine/core";

const theme = createTheme({
    colors: {
        accent: [
            "var(--accent-bg)",
            "var(--accent-bg)",
            "var(--accent-bg)",
            "var(--accent-bg)",
            "var(--accent-bg)",
            "var(--accent-bg)",
            "var(--accent-bg)",
            "var(--accent-bg)",
            "var(--accent-bg)",
            "var(--accent-bg)",
        ],
    },
    primaryColor: "accent",
});

export default theme;
