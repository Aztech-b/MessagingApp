import { createTheme } from "@mantine/core";

const theme = createTheme({
    colors: {
        accent: [
            "var(--accent)",
            "var(--accent)",
            "var(--accent)",
            "var(--accent)",
            "var(--accent)",
            "var(--accent)",
            "var(--accent)",
            "var(--accent)",
            "var(--accent)",
            "var(--accent)",
        ],
    },
    primaryColor: "accent",
});

export default theme;
